# Next.js Router Migration Guide\n\n## Issue\n\nBuild failure: App Router and Pages Router both match path `/api/ai`\n\n## Solution\n\nMigrate from **Pages Router** (legacy) to **App Router** (current standard).\n\n### Step 1: Backup Current Structure\n\n```bash\n# Archive pages directory\ngit mv pages pages.backup\n```\n\n### Step 2: Create App Router Structure\n\n```\napp/\n├── layout.tsx              # Root layout\n├── page.tsx                # Home page (/)\n├── api/\n│   ├── ai/\n│   │   └── route.ts        # POST /api/ai\n│   ├── analytics/\n│   │   └── route.ts        # POST /api/analytics\n│   ├── wallet/\n│   │   └── route.ts        # GET /api/wallet\n│   ├── news/\n│   │   └── route.ts        # GET /api/news\n│   └── ... [other routes]\n├── dashboard/\n│   └── page.tsx            # /dashboard\n├── vault/\n│   └── page.tsx            # /vault\n└── ... [other pages]\n```\n\n### Step 3: Migrate API Routes\n\n**Old (Pages Router):** `pages/api/ai.js`\n```javascript\nexport default async (req, res) => {\n  if (req.method === 'POST') {\n    res.json({ result: 'success' });\n  }\n}\n```\n\n**New (App Router):** `app/api/ai/route.ts`\n```typescript\nimport { NextRequest, NextResponse } from 'next/server';\n\nexport async function POST(request: NextRequest) {\n  return NextResponse.json({ result: 'success' });\n}\n```\n\n### Step 4: Migrate Pages\n\n**Old (Pages Router):** `pages/dashboard.js`\n```javascript\nexport default function Dashboard() {\n  return <div>Dashboard</div>;\n}\n```\n\n**New (App Router):** `app/dashboard/page.tsx`\n```typescript\nexport default function Dashboard() {\n  return <div>Dashboard</div>;\n}\n```\n\n### Step 5: Update Imports\n\n- Replace `next/router` → `next/navigation`\n- Replace `useRouter()` from pages → `useRouter()` from app\n\n### Step 6: Test Build\n\n```bash\nnpm run build\n# Should complete without routing conflicts\n```\n\n## Resources\n\n- [Next.js App Router Migration](https://nextjs.org/docs/app/building-your-application/upgrading/app-router-migration)\n- [API Routes → Route Handlers](https://nextjs.org/docs/app/building-your-application/routing/route-handlers)\n